### Rust 编码检查清单

**1. 工具与格式**
* 代码是否通过 `cargo fmt` 格式化。
* 代码是否通过 `cargo clippy` 检查且无告警。
* 命名是否符合规范：类型名为 `PascalCase`，函数与变量名为 `snake_case`，常量为 `SCREAMING_SNAKE_CASE`。

**2. 内存与性能**
* 是否消除了不必要的 `.clone()` 调用。
* 接口参数是否优先使用切片（`&str`, `&[T]`）而非所有权类型（`String`, `Vec`）。
* 是否优先使用迭代器（Iterator）而非手动索引循环。
* 集合类型是否在已知大小时使用了 `with_capacity` 预分配内存。

**3. 错误处理**
* 库代码中是否严禁使用 `unwrap()` 或 `expect()`。
* 错误处理是否优先使用 `?` 操作符传播。
* 是否为自定义错误实现了 `std::error::Error`（或使用 `thiserror`）。
* 是否使用 `Result` 处理可恢复错误，仅在不可恢复的逻辑错误处使用 `panic!`。

**4. 类型设计**
* 是否为结构体实现了或派生了常用的 Trait（`Debug`, `Default`, `PartialEq`）。
* 公开接口是否使用了 `impl Trait` 或泛型约束来增加灵活性。
* 内部字段是否遵循最小可见性（优先使用 `pub(crate)` 而非 `pub`）。
* 是否提供了 `new()` 构造函数或实现了 `Default`。

**5. 模式匹配**
* 是否使用 `match` 或 `if let` 显式处理所有 `Option` 和 `Result` 情况。
* 是否使用了组合子（`.map()`, `.and_then()`）简化逻辑。
* 匹配分支较多时，是否提取了公共逻辑以保持代码紧凑。

**6. 文档与注释**
* 所有公共 API 是否具备 `///` 文档注释。
* 文档中是否包含必要的示例代码块。
* 使用 `unsafe` 代码块时，是否附带 `// SAFETY:` 理由说明。
