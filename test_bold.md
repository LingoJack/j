# 加粗样式测试文档

## 这是一段包含加粗的文本

**这是完全加粗的段落。** 在浅色终端下，加粗文字的颜色应该清晰可读，不应该和背景色混在一起。

这是一段普通文本，其中包含 **行内加粗** 的部分。加粗前后的普通文字应该都正常显示。

## 各种加粗场景

### 1. 独立加粗段落

**加粗的文字应该使用终端默认前景色**，在浅色背景下呈现为深色。

### 2. 混合内容

这是 **重要提示**：加粗和普通文字之间的过渡应该自然流畅。**不要忽略这一点**。

### 3. 列表中的加粗

- **加粗的列表项** - 后面跟普通说明文字
- 第二项 **关键内容** 在中间
- 整项都是 **加粗状态**

### 4. 引用块中的加粗

> **加粗的引用内容**，后面跟着普通引用文字。
> 普通引用文字，**再加粗**。

### 5. 行内代码与加粗混合

在代码中 `**这不是加粗**` 只是普通代码文本。但 `varname` 变量名配合 **加粗说明** 应该都能看清。

### 6. 链接加粗

[**加粗链接文本**](https://example.com) 应该同时具备链接颜色和加粗效果。

## 代码块中的加粗

```
这个代码块里的 **文字不会加粗**，因为代码块不解析 markdown 格式。
```

## 表格中的加粗

| 列A | 列B | 列C |
|-----|-----|-----|
| **加粗值** | 普通 | **加粗** |
| 普通 | **加粗** | 普通 |

---

## 总结

以上各种场景下，**所有加粗文字**都应当清晰可辨，不与浅色背景混淆。
