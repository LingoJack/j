## 1. 数据结构准备

- [x] 1.1 在 `TodoApp` 结构体中新增 `preview_scroll: u16` 字段，用于跟踪预览区滚动偏移
- [x] 1.2 在 `TodoApp` 结构体中新增 `last_preview_index: Option<usize>` 字段，用于检测选中项变化并重置滚动

## 2. 显示宽度计算

- [x] 2.1 引入 `unicode_width` crate 依赖（如果尚未添加），用于精确计算多字节字符显示宽度
- [x] 2.2 添加 `count_wrapped_lines()` 函数，计算文本在指定宽度下换行后的行数（参考 `editor.rs` 中的实现）

## 3. 布局动态调整

- [x] 3.1 修改 `draw_ui` 函数，根据选中项内容宽度动态决定是否显示预览区
- [x] 3.2 实现布局约束切换：无预览区时列表区占满，有预览区时列表 55% + 预览区 Min(5)

## 4. 预览区渲染

- [x] 4.1 实现预览区标题逻辑：显示选中项索引和创建时间（格式：`📖 第 N 项预览 [YYYY-MM-DD]`）
- [x] 4.2 实现滚动进度显示：长文本时标题显示 `[X/Y行] Alt+↓/↑滚动`
- [x] 4.3 使用 `Paragraph::wrap()` 渲染预览区内容，支持自动换行

## 5. 滚动功能

- [x] 5.1 在 TUI 主循环中处理 Alt+↑/↓ 快捷键，实现预览区滚动
- [x] 5.2 实现切换选中项时自动重置 `preview_scroll` 为 0
- [x] 5.3 实现滚动边界钳制：`preview_scroll` 不超过 `max(0, total_wrapped - preview_height)`

## 6. 文档更新

- [x] 6.1 更新 `assets/help.md`，在 Todo 待办备忘录章节添加预览区功能说明和 Alt+↑/↓ 快捷键
- [x] 6.2 更新 `README.md`，在待办备忘录模块说明中添加预览区特性描述
