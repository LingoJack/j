# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# 注意：dist/ **不**忽略 —— 构建产物会被 rust-embed 编译进 j 二进制，
# 必须随源码一起提交，否则 clone 后无法直接 cargo build。
# 重新生成：`make build-reader-web` 或 `cd assets/reader && npm run build`。
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
